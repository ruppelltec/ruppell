name: Deploy Vite App

on:
  push:
    branches: [ main ]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies
        run: npm ci

      - name: Build Vite app
        run: npm run build

      - name: Build Docker image
        run: docker build -t vite-app .

      - name: Set compose command
        id: composecmd
        run: |
          if command -v docker-compose &> /dev/null; then
            echo "cmd=docker-compose" >> $GITHUB_OUTPUT
          else
            echo "cmd=docker compose" >> $GITHUB_OUTPUT
          fi

      - name: Deploy Ruppell
        run: |
          ${{ steps.composecmd.outputs.cmd }} down || true
          docker container prune -f || true

      # --- Clean up unused images ---
      - name: Clean up Docker images
        run: |
          docker image prune -f || true

      # --- Build + start ---
      - name: Build and start Ruppell app
        run: |
          ${{ steps.composecmd.outputs.cmd }} up --build -d

      # --- Wait for app to be ready ---
      - name: Wait for app to start
        run: |
          echo "Waiting for Ruppell app to start..."
          sleep 10
          timeout 60 bash -c 'until ${{ steps.composecmd.outputs.cmd }} ps | grep "healthy\|running"; do sleep 2; done' || true

      # --- Verify deployment ---
      - name: Verify deployment
        run: |
          echo "===== Container Status ====="
          ${{ steps.composecmd.outputs.cmd }} ps
          echo "===== Health Check ====="
          curl -f http://localhost:80 || echo "Health check failed"

      # --- Logs on failure ---
      - name: Show service logs if failed
        if: failure()
        run: |
          echo "===== Containers ====="
          ${{ steps.composecmd.outputs.cmd }} ps
          echo "===== App Logs ====="
          ${{ steps.composecmd.outputs.cmd }} logs --tail=100
